package com.seek.generation.sandbox.objects;

import com.badlogic.gdx.graphics.g3d.Model;

public class ConeObject extends GameObject {
    public ConeObject(Model model) {
        super(model);
    }
}
